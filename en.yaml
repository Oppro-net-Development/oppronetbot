en:
  # Uncategorized
  announce:
    name: announce
    options:
      title:
        description: Überschrift

  # Warn
  add_warning:
    name: warn
    description:
    options:
      member:
        name: user
        description: Select a user
      reason:
        name: reason
        description: choose a reason

  remove_warning:
    name: remove-warning
    options:
      member:
        name: user
        description: Select the user
      reason:
        name: reason
        description: Choose a reason

  warnings:
    name: warnings
    description: Show all warnings of a user from this server
    options:
      member:
      name: user

  # Other moderation
  purge:
    name: delete
    description: Delete messages from a channel
    options:
      amount:
      name: number

  # Timeouts
  timeout:
    name: timeout
    description: Timeout a user
    options:
      member:
      name: user

  remove_timeout:
    name: remove-timeout
    description: Remove a timeout